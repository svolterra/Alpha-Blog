<%= gravatar_for @user, size: 170 %>

<h2 class="text-center mt-3"><%= @user.username %>'s Profile</h2>

<div class="text-center mt-4">
  <%= link_to "Edit Profile", edit_user_path(@user), class: "btn btn-outline-dark" %>
</div>

<%= render 'articles/article' %>